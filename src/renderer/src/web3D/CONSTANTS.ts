import VEngine from "@renderer/assets/build/VEngine.module";

const CONSTANTS: {
    viewer: VEngine.Viewer;
    scene: VEngine.Scene;
    camera: VEngine.Camera;
    drawHelper: VEngine.DrawHelper;
    eventHandler: VEngine.ScreenSpaceEventHandler;
} = {} as any;

export default CONSTANTS;
