import styled from "styled-components";

export const Wrapper = styled.div`
    z-index: 10;
    width: 315px;
    height: 470px;
    background-color: rgba(0, 0, 0, 0.5);

    cursor: pointer;

    position: absolute;
`;
